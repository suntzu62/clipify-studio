REDIS_URL=redis://default:c3NlSqpGqpWT2BPSrlWRTtMQqsCi0zuX@redis-14366.c256.us-east-1-2.ec2.redns.redis-cloud.com:14366
WORKERS_CONCURRENCY=4
# Per-queue concurrency (overrides WORKERS_CONCURRENCY) - OPTIMIZED FOR SPEED
INGEST_CONCURRENCY=2
TRANSCRIBE_CONCURRENCY=4
SCENES_CONCURRENCY=4
RANK_CONCURRENCY=4
RENDER_CONCURRENCY=4
TEXTS_CONCURRENCY=4
EXPORT_CONCURRENCY=2
WORKERS_API_KEY=dev-key
WORKERS_API_PORT=8787

# Supabase Configuration
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_STORAGE_BUCKET=raw

# OpenAI Configuration
OPENAI_API_KEY=
TRANSCRIBE_MODEL=whisper-1            # optional: gpt-4o-transcribe | gpt-4o-mini-transcribe

# Texts Configuration (YouTube + Blog)
TEXTS_MODEL=gpt-4o-mini
TEXTS_TONE=informal-claro          # voz/estilo do texto
TEXTS_HASHTAG_MAX=12               # máximo de hashtags por clipe (3–12); hard cap global 60
TEXTS_BLOG_WORDS_MIN=800
TEXTS_BLOG_WORDS_MAX=1200

# YT-DLP Configuration
YTDLP_COOKIES_PATH=   # optional path to cookies.txt
FFMPEG_PATH=          # uses ffmpeg-static by default

# Scenes Configuration
SCENES_SILENCE_DB=-35
SCENES_SILENCE_MIN=0.3
SCENES_SIM_THRESHOLD=0.85
SCENES_MIN=30
SCENES_MAX=90

# Rank Configuration
RANK_TOP_K=12
RANK_MIN=30
RANK_MAX=90
RANK_TARGET=55
RANK_EMBED_MODEL=text-embedding-3-small

# Render Configuration - OPTIMIZED FOR SPEED
RENDER_FPS=30
RENDER_MODE=crop            # crop | fit_blur
RENDER_VIDEO_BITRATE=6M     # optimized for speed
RENDER_VIDEO_MAXRATE=8M     # reduced for faster encoding
RENDER_VIDEO_BUFSIZE=12M    # smaller buffer for faster processing
RENDER_AUDIO_BITRATE=128k   # slightly lower for speed
RENDER_FONT=Inter           # TODO: garantir fonte disponível
RENDER_MARGIN_V=60

# YouTube OAuth (Export)
# OBS: uploads via API consomem ~1600 unidades de quota por vídeo.
# Apps não verificados costumam forçar PRIVADO por padrão, mesmo que privacy=unlisted/public.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
YT_REDIRECT_URI=
YT_SCOPES=https://www.googleapis.com/auth/youtube.upload

# YouTube Upload Defaults
YT_PRIVACY=unlisted          # private | unlisted | public
YT_CATEGORY_ID=22            # People & Blogs
YT_MADE_FOR_KIDS=false

# ----------------------------------------------
# Analytics (PostHog) & Demo E2E
# ----------------------------------------------
POSTHOG_KEY=
POSTHOG_HOST=https://app.posthog.com

# Demo E2E (YouTube vídeo ≥10 min)
DEMO_YT_URL=https://www.youtube.com/watch?v=<video>
DEMO_NEEDED_MINUTES=10

# Rate limits (external APIs)
OPENAI_RATE_MAX=10
OPENAI_RATE_MS=1000
YOUTUBE_RATE_MAX=10
YOUTUBE_RATE_MS=1000
